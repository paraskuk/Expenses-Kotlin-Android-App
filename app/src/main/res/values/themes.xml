<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for light mode -->
    <style name="Base.Theme.Assignment_paras" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/lightBackground</item>
        <item name="android:textColor">@color/lightText</item>
    </style>

    <!-- Text appearance for menu items in light theme -->
    <style name="TextAppearance.MenuItem.Light" parent="TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:textColor">@color/menuText</item>
    </style>

    <!-- Base application theme -->
    <style name="Theme.Assignment_paras" parent="Base.Theme.Assignment_paras" />
</resources>
